describe('Procrastinate', function () {
  context('with an empty input', function () {
    it('returns an empty string');
  });

  context('with an input representing pending Mocha specs', function () {
    it('returns pending Mocha specs');
  });
});
